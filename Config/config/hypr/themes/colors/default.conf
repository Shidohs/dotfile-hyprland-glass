# Default theme colors for Hyprland

# Window colors
$active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
$inactive_border = rgba(595959aa)

# Workspace colors
$active_workspace = rgba(33ccffee)
$inactive_workspace = rgba(595959aa)
$urgent_workspace = rgba(ff0000ee)

# Bar colors
$bar_background = rgba(1a1a1aee)
$bar_foreground = rgba(ffffffee)

# Notification colors
$notification_background = rgba(1a1a1aee)
$notification_foreground = rgba(ffffffee)
$notification_error = rgba(ff0000ee)
$notification_warning = rgba(ffff00ee)

# Menu colors
$menu_background = rgba(1a1a1aee)
$menu_foreground = rgba(ffffffee)
$menu_selected = rgba(33ccffee)
